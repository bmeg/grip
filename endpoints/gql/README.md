
## Notes for building code

```
curl -O https://www.antlr.org/download/antlr-4.13.2-complete.jar
```

```
./generate.sh
```

## Compiler Mapping Guide

For the currently supported GQL subset and its GripQL translation mapping, see `GQL_TO_GRIPQL.md`.

For current implementation status and prioritized remaining TODOs, see `GQL_COMPILER_PROGRESS.md`.
